"""Framework-agnostic core for refined-types.

Zero dependencies beyond typing-extensions. Defines the ``Refined`` marker,
``Predicate`` base, ``Result`` type, and ``parse`` / ``try_parse`` entry points.

The real implementation lands in 0.1.0.
"""
